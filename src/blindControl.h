#include "esphome.h"
#include "Interfaccia.h" 

class MyCustomComponent : public Component {
 public:
  void setup() override {
    // Setup the component



  }
  void loop() override {
    // This is called every loop


    
  }
};